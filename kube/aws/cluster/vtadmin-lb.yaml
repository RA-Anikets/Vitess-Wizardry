apiVersion: v1
kind: Service
metadata:
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-internal: "true"
    service.beta.kubernetes.io/aws-load-balancer-type: nlb
  labels:
    planetscale.com/cell: zone1
    planetscale.com/cluster: cmms-vitess-cluster
    planetscale.com/component: vtadmin
  name: test-internal-vtadmin
  namespace: cmms
spec:
  ports:
  - port: 443
    protocol: TCP
    targetPort: 15000
  selector:
    planetscale.com/cell: zone1
    planetscale.com/cluster: cmms-vitess-cluster
    planetscale.com/component: vtadmin
  type: LoadBalancer